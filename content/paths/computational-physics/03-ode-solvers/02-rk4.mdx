---
title: 'RK4, and where the magic numbers come from'
blurb: 'Four slope samples per step, weighted 1-2-2-1, and suddenly the error falls as the fourth power. The coefficients are not arbitrary — they are what you get when you demand that error terms cancel.'
order: 2
tier: 'core'
minutes: 30
teaches:
  - 'runge-kutta'
  - 'heun-method'
  - 'rk4'
  - 'butcher-tableau'
requires:
  - 'forward-euler'
  - 'local-truncation-error'
  - 'global-error'
  - 'central-difference'
status: 'live'
updated: 2026-09-08
---

Forward Euler's problem is that it commits to the slope measured at the starting point
and uses it for the whole interval. If the slope changes across the step — and it always
does — that is wrong from the moment you leave.

The Runge–Kutta idea is to **look before you leap**: take trial steps inside the
interval, sample the slope there, and combine the samples so that the low-order error
terms cancel.

## Two samples: Heun

Step out with Euler to get a provisional endpoint, sample the slope *there*, then use
the average of the two slopes:

$$
\begin{aligned}
k_1 &= f(t_n, y_n) \\
k_2 &= f(t_n + h,\; y_n + h k_1) \\
y_{n+1} &= y_n + \tfrac{h}{2}(k_1 + k_2)
\end{aligned}
$$

This is the trapezoid rule applied to time-stepping, and it is second order. The
reason it works is the same symmetry argument that made the central difference second
order: averaging a slope at the start with a slope at the end cancels the term that a
one-sided estimate leaves behind.

<Predict
  id="rk-cost-vs-order"
  question="Heun costs two f-evaluations per step; forward Euler costs one. At the same total number of f-evaluations, which is more accurate on a smooth problem?"
  options={[
    { key: 'a', label: 'Euler — it takes twice as many steps for the same budget', why: 'It does take twice as many steps, but its error falls only as h¹. Halving h halves the error, while the order-2 method gains a factor of 4 from the same halving.' },
    { key: 'b', label: 'Heun, by a wide margin at small tolerances', why: 'Correct. With N evaluations, Euler achieves error ~ 1/N and Heun ~ 1/N². The gap widens without limit as the tolerance tightens — which is why higher order is almost always worth the per-step cost.' },
    { key: 'c', label: 'They are equivalent — order and cost cancel exactly', why: 'This would need the error to scale the same way in both, which it does not: one is h¹ and the other h².' },
    { key: 'd', label: 'Depends entirely on the problem', why: 'The problem matters for stiffness and smoothness, but for a smooth non-stiff problem the asymptotic comparison is decisive.' },
  ]}
  correct="b"
>

Switch the plot below to **vs work** — that divides out the per-step cost and compares
methods at equal f-evaluations, which is the only fair axis.

<ConvergenceLab
  problem="decay"
  methods={['forward-euler', 'heun', 'midpoint', 'rk4']}
  initial={['forward-euler', 'heun', 'rk4']}
  caption="Toggle 'vs work' to compare at equal cost rather than equal step size. Higher order wins so decisively that the per-step expense barely registers."
/>

</Predict>

## Four samples: the classic

RK4 samples four times — once at the start, twice at the midpoint, once at the end —
and weights them $1:2:2:1$:

$$
\begin{aligned}
k_1 &= f(t_n,\; y_n) \\
k_2 &= f(t_n + \tfrac{h}{2},\; y_n + \tfrac{h}{2}k_1) \\
k_3 &= f(t_n + \tfrac{h}{2},\; y_n + \tfrac{h}{2}k_2) \\
k_4 &= f(t_n + h,\; y_n + h k_3) \\[4pt]
y_{n+1} &= y_n + \frac{h}{6}\left(k_1 + 2k_2 + 2k_3 + k_4\right)
\end{aligned}
$$

Those weights look like folklore. They are not. Expand the numerical step as a Taylor
series in $h$, expand the true solution the same way, and demand that the two agree
through $h^4$. That gives a system of **order conditions** on the coefficients, and
$1/6, 1/3, 1/3, 1/6$ is a solution. The $1:2:2:1$ pattern is Simpson's rule — which is
not a coincidence, since for $f$ depending on $t$ alone, RK4 *is* Simpson's rule.

If you have met Simpson's rule for integration, you have already met the weights.

<ConvergenceLab
  problem="oscillator"
  methods={['forward-euler', 'heun', 'rk4']}
  initial={['rk4']}
  hMax={0.5}
  count={11}
  caption="RK4's measured slope on the harmonic oscillator. Note the dimmed points at both ends: the coarse steps are pre-asymptotic (the h⁴ term does not dominate yet) and the finest ones have hit the roundoff floor. Only the middle is fitted — see below."
/>

That plot is worth reading carefully, because it contains the previous chapter's lesson
in a new guise. The dimmed points at the left are **pre-asymptotic**: at large $h$ the
neglected $h^5$ and higher terms are not yet negligible, so the slope is not 4 there. The
dimmed points at the right have hit the **roundoff floor** from the last chapter — around
$10^{-14}$, where the truncation error is finally smaller than the noise in the
arithmetic.

Fitting a line through *all* the points would give an answer near 4.6, which is wrong.
The order is a property of the asymptotic regime, and measuring it means finding that
regime first. The platform does this by computing the order between each consecutive
pair and fitting only over the longest run where those local orders agree.

<Recall id="rk4-weights" concept="rk4">
  <div slot="front">Where do RK4's <code>1/6, 1/3, 1/3, 1/6</code> weights come from, and why is RK4 fourth order rather than fifth?</div>

They come from **order conditions**. Expand the numerical step in powers of $h$, expand
the exact solution in powers of $h$, and require the two to agree through $h^4$. That
imposes a system of polynomial equations on the stage coefficients and weights; the
classical $1:2:2:1$ scheme is one solution.

It stops at fourth order because the $h^5$ conditions cannot also be satisfied with only
four stages. In fact the number of stages needed grows faster than the order beyond this
point — four stages buy order 4, but order 5 needs six stages, and order 8 needs eleven.
Four stages for fourth order is the last point where the exchange rate is one-for-one,
which is a large part of why RK4 became *the* default.

</Recall>

## Build it

<CodeChallenge
  id="rk4-implement"
  prompt="Implement one RK4 step. You have f(t, y) → array, t, y (array) and h. Compute the four stages and return the weighted combination."
  starter={`const k1 = f(t, y);
const k2 = f(t + h / 2, y.map((yi, i) => yi + (h / 2) * k1[i]));
// k3 uses the midpoint again, but with k2's slope
// k4 uses the full step, with k3's slope

// combine: y + (h/6)(k1 + 2*k2 + 2*k3 + k4)
return y.map((yi, i) => yi + h * k1[i]);   // ← this is just Euler; fix it`}
  expectedOrder={4}
  hint="k3 mirrors k2 but steps with k2 instead of k1. k4 steps a full h using k3. Then weight them 1, 2, 2, 1 and divide by 6."
  solution={`const k1 = f(t, y);
const k2 = f(t + h / 2, y.map((yi, i) => yi + (h / 2) * k1[i]));
const k3 = f(t + h / 2, y.map((yi, i) => yi + (h / 2) * k2[i]));
const k4 = f(t + h,     y.map((yi, i) => yi + h * k3[i]));

return y.map((yi, i) => yi + (h / 6) * (k1[i] + 2 * k2[i] + 2 * k3[i] + k4[i]));`}
/>

<Tier level="advanced" title="Butcher tableaux, and reading a method off a table">

Every explicit Runge–Kutta method has the same shape:

$$
k_i = f\left(t_n + c_i h,\; y_n + h\sum_{j<i} a_{ij} k_j\right), \qquad
y_{n+1} = y_n + h \sum_i b_i k_i
$$

so a method *is* its coefficients $(A, b, c)$. The **Butcher tableau** writes them down:

$$
\begin{array}{c|cccc}
0 & & & & \\
\tfrac12 & \tfrac12 & & & \\
\tfrac12 & 0 & \tfrac12 & & \\
1 & 0 & 0 & 1 & \\
\hline
 & \tfrac16 & \tfrac13 & \tfrac13 & \tfrac16
\end{array}
$$

Left column is $c$ (when to sample), the block is $A$ (how to get there), the bottom row
is $b$ (how to combine). Strictly lower-triangular $A$ means explicit — each stage uses
only earlier ones. Fill the upper triangle and the method becomes implicit, which is the
next lesson.

Two conditions are worth internalising:

- $\sum_j a_{ij} = c_i$ — each stage is evaluated at a consistent time. Nearly every method satisfies this.
- $\sum_i b_i = 1$ — the weights average the slopes rather than scaling them. **Any method violating this is not even first order**, and it is the first thing to check when a hand-written integrator measures order 0.

**Embedded pairs** are the practical payoff. Choose two weight rows $b$ and $\hat b$ over
the *same* stages, giving methods of order $p$ and $p-1$. Their difference estimates the
local error for almost no extra cost, and that estimate drives adaptive step size
control. Dormand–Prince 5(4) — the `ode45` of MATLAB and the default in SciPy — is
exactly this: seven stages, two weight rows, order 5 with a free order-4 error estimate.

</Tier>

<Tier level="frontier" title="when RK4 is the wrong default">

RK4 is an excellent general-purpose method and a poor special-purpose one.

- **Stiff problems.** RK4's stability region is bounded, so a stiff system still forces a
  tiny $h$. No explicit method escapes this. → implicit methods, BDF.
- **Long-time Hamiltonian integration.** RK4 conserves nothing. Over a long orbit its
  energy drifts secularly, and a *second*-order symplectic method beats it comprehensively.
  This is the next chapter, and it is the most counterintuitive result in the path.
- **Fixed step in general.** Real solvers adapt. RK4 with fixed $h$ wastes effort where
  the solution is smooth and under-resolves where it is not.
- **Highly oscillatory problems**, where exponential integrators or Magnus methods
  exploit structure RK4 cannot see.

The habit worth building is not "use RK4" but: *what do I know about this problem —
stiffness, conserved quantities, smoothness — and which method exploits that?*

</Tier>

## What to carry forward

- Order comes from **cancellation**, engineered by choosing coefficients to kill Taylor terms.
- Higher order beats smaller steps at equal cost, on smooth problems, by a widening margin.
- **Always measure the order you actually got.** The measurement is the test suite for a
  numerical method, and it is how you catch a subtly wrong stage.
- Order is measured in the asymptotic regime — not too coarse, not below the roundoff floor.
