---
// @graded — when given an `id`, the scene checks itself.
import Island, { type BatTheBallProps } from '../viz/BatTheBall.tsx';
const props = Astro.props as BatTheBallProps;
---
<Island client:visible {...props} />
