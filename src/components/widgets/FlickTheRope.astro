---
// @graded — when given `id` and `targetRatio`, the scene checks itself.
import Island, { type FlickTheRopeProps } from '../viz/FlickTheRope.tsx';
const props = Astro.props as FlickTheRopeProps;
---
<Island client:visible {...props} />
