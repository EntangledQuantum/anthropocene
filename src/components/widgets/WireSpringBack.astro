---
// @graded — with `id`, the scene checks itself: the hardest pull the wire fully recovers from.
import Island, { type WireSpringBackProps } from '../viz/WireSpringBack.tsx';
const props = Astro.props as WireSpringBackProps;
---
<Island client:visible {...props} />
