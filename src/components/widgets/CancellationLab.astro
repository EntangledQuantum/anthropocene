---
// Wrapper so lessons can write <CancellationLab /> with no import and no client directive.
// MDX components supplied through the `components` prop cannot carry
// client:* directives themselves, so the directive lives here.
import Island from '../viz/CancellationLab.tsx';
---
<Island client:visible {...Astro.props} />
