---
import Island, { type TierLevel } from '../learn/Tier.tsx';

interface Props { level: TierLevel; title?: string }
const props = Astro.props;
---
<Island client:visible {...props}>
  <slot />
</Island>
