---
// @graded — with `id`, find the slowest steady wind at which the deck's twist grows.
import Island, { type FlutterDeckProps } from '../viz/FlutterDeck.tsx';
const props = Astro.props as FlutterDeckProps;
---
<Island client:visible {...props} />
