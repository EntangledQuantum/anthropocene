---
// @graded — with `id`, the scene checks itself.
import Island, { type LoadTheBargeProps } from '../viz/LoadTheBarge.tsx';
const props = Astro.props as LoadTheBargeProps;
---
<Island client:visible {...props} />
