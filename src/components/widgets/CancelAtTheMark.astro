---
// @graded — with `id`, the scene checks itself.
import Island, { type CancelAtTheMarkProps } from '../viz/CancelAtTheMark.tsx';
const props = Astro.props as CancelAtTheMarkProps;
---
<Island client:visible {...props} />
