---
// @graded — with `id`, tilt the top ramp until the race is a tie.
import Island, { type RollingRaceProps } from '../viz/RollingRace.tsx';
const props = Astro.props as RollingRaceProps;
---
<Island client:visible {...props} />
