---
// @graded — when given `id`, the scene checks itself. Chapter 7.
import Island, { type StayPutCartProps } from '../viz/StayPutCart.tsx';
const props = Astro.props as StayPutCartProps;
---
<Island client:visible {...props} />
