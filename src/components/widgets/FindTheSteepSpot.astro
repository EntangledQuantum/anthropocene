---
// @graded — park the probe where the field along the track is strongest.
import Island, { type FindTheSteepSpotProps } from '../viz/FindTheSteepSpot.tsx';
const props = Astro.props as FindTheSteepSpotProps;
---
<Island client:visible {...props} />
