---
// @graded — when given an `id`, the scene checks itself.
import Island, { type DriveTheStringProps } from '../viz/DriveTheString.tsx';
const props = Astro.props as DriveTheStringProps;
---
<Island client:visible {...props} />
