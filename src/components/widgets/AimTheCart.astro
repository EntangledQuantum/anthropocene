---
// @graded — when given an `id`, the scene checks itself.
import Island, { type AimTheCartProps } from '../viz/AimTheCart.tsx';
const props = Astro.props as AimTheCartProps;
---
<Island client:visible {...props} />
