---
// @graded — when given `id`, the scene checks itself.
import Island, { type CartThroughWindProps } from '../viz/CartThroughWind.tsx';
const props = Astro.props as CartThroughWindProps;
---
<Island client:visible {...props} />
