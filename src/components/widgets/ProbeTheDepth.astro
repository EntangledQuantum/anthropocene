---
// @graded — with `id`, `mark` and `probeIn`, the scene checks itself.
import Island, { type ProbeTheDepthProps } from '../viz/ProbeTheDepth.tsx';
const props = Astro.props as ProbeTheDepthProps;
---
<Island client:visible {...props} />
