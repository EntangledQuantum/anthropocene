---
// @graded — with `id` and `target`, the scene checks itself.
import Island, { type SqueezeTheSleeveProps } from '../viz/SqueezeTheSleeve.tsx';
const props = Astro.props as SqueezeTheSleeveProps;
---
<Island client:visible {...props} />
