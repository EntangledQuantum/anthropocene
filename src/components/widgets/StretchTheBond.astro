---
// @graded — when given `id`, the scene checks itself. Chapter 7.
import Island, { type StretchTheBondProps } from '../viz/StretchTheBond.tsx';
const props = Astro.props as StretchTheBondProps;
---
<Island client:visible {...props} />
