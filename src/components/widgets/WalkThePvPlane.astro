---
// @graded — with `id` and `target`, the scene checks itself.
import Island, { type WalkThePvPlaneProps } from '../viz/WalkThePvPlane.tsx';
const props = Astro.props as WalkThePvPlaneProps;
---
<Island client:visible {...props} />
