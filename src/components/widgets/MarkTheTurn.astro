---
// @graded — when given `id`, the scene checks itself. Chapter 7.
import Island, { type MarkTheTurnProps } from '../viz/MarkTheTurn.tsx';
const props = Astro.props as MarkTheTurnProps;
---
<Island client:visible {...props} />
