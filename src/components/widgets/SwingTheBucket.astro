---
// @graded — when given `id`, the scene checks itself: the slowest speed that keeps the water in.
import Island, { type SwingTheBucketProps } from '../viz/SwingTheBucket.tsx';
const props = Astro.props as SwingTheBucketProps;
---
<Island client:visible {...props} />
